#!/usr/bin/env bash

BINPATH=`readlink -f $0`
BINDIR=
APPHOME=`dirname \`dirname $BINPATH \``

java -Dlogback.configurationFile=$APPHOME/cfg/logback.xml \
     -Dapp.home=$APPHOME \
     -jar $BINPATH/${artifactId}.jar $@
